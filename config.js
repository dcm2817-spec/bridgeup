// BridgeUp — Supabase config
// The publishable key is safe to expose in client code — it's designed for
// that, and access is controlled by your Row Level Security policies (see
// schema.sql). NEVER put the secret key (sb_secret_...) in this file or any
// client-side code — it bypasses RLS completely.

export const SUPABASE_URL = "https://mwuyjaekewcqccfcltwb.supabase.co";
export const SUPABASE_PUBLISHABLE_KEY = "sb_publishable_L_XeDGF8JKxOnxQ-l_fmug_7WT7qyf7";
